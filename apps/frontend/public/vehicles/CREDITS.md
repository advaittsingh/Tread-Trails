# Vehicle hub photos

Images are stored under `public/vehicles/{slug}/` and sourced from [Wikimedia Commons](https://commons.wikimedia.org/) (Creative Commons). See `manifest.json` for per-file titles and licenses.

To refresh assets: `npm run fetch:vehicle-images`
